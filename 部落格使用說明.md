# NeoWise 部落格使用說明

這套 Blog 系統把「AI 產文 → SEO → 圖片需求 → 審核 → 發布」整合成以 Markdown 檔為中心的流程。
文章寫成 `.md`，用產生器轉成靜態 HTML，SEO 最乾淨，跟現有官網風格一致。

---

## 一、整體流程

```
輸入題目
  → (在 Kiro 對話裡) 我產生文章草稿 .md（含正文 + SEO + 圖片需求）
  → 你審核（看檔案、要改就說）
  → 準備圖片（依「圖片需求」放進 assets 資料夾）
  → 執行產生器（雙擊 build-blog.bat）
  → 用 GitHub Desktop 同步到 GitHub → 上線
```

---

## 二、資料夾結構

```
neowise-website/
├── blog/                       ← 產生器產出的成品（會上線）
│   ├── index.html              ← 文章列表頁（自動產生）
│   ├── <slug>.html             ← 各篇文章（自動產生）
│   └── assets/<slug>/          ← 各篇文章的圖片（你手動放）
├── blog-content/               ← 文章原始碼（Markdown）
│   ├── _drafts/                ← 草稿、待審核
│   └── _published/             ← 已發布歸檔（可選）
├── scripts/build-blog.mjs      ← 產生器程式
├── build-blog.bat              ← 雙擊執行產生器
└── sitemap.xml                 ← 自動更新
```

> **重點**：`blog/` 裡的 HTML 都是「自動產生」的，不要手動改。要改內容改 `blog-content/` 的 `.md`，再重跑產生器。

---

## 三、日常操作步驟

### Step 1 — 產生文章（模式 1）

在 Kiro 對話裡給題目，例如：

> 幫我寫一篇部落格：「機器視覺對位在半導體封裝的應用」

我會在 `blog-content/_drafts/` 產生一個 `.md`，包含：
- 文章正文
- SEO（title / description / keywords / slug）
- 圖片需求清單（每張圖的位置、用途、alt、生成提示）

### Step 2 — 審核

打開 `blog-content/_drafts/<slug>.md` 檢視。要改就直接跟我說（改標題、改語氣、加段落都行）。
審核通過後，把檔案最上方的 `status` 改成 `approved`：

```
status: "approved"     # draft=不發布，approved/published=會發布
```

（`draft` 的文章產生器會略過、不會上線。）

### Step 3 — 準備圖片

依 `.md` 裡 `images` 清單，把圖片放到 `blog/assets/<slug>/`，檔名要一致。
圖片來源可以是：自己拍、免費圖庫、或用 AI 生圖工具照 `prompt` 產生。
沒有圖也能先發（版面會自動略過缺圖），之後補上再重跑即可。

**免費、可商用的圖片來源：**

照片（真實照片，適合封面圖）：
- [Unsplash](https://unsplash.com)：品質最高、量最大，免費商用免標註
- [Pexels](https://pexels.com)：照片+影片，免費商用免標註
- [Pixabay](https://pixabay.com)：照片、插圖、向量圖，免費商用免標註
- 搜尋建議用英文：`machine vision`、`factory automation`、`circuit board`、`industrial robot`、`AI technology`、`semiconductor`

插圖／資訊圖（適合示意圖，如三種感測比較、瑕疵類型）：
- [Canva](https://canva.com)：免費帳號可做資訊圖，有大量科技風範本，套版改字最快
- [unDraw](https://undraw.co)：免費可商用向量插圖，可改主色（設成品牌紫 #a855f7）
- [Freepik](https://freepik.com)：向量與資訊圖素材多（免費方案部分需標註或付費）

圖示 icon：
- [Flaticon](https://flaticon.com)、[Icons8](https://icons8.com)

**注意事項：**
- 商用前確認授權（少數標「免費」者僅限個人用途）。
- 避免使用含明顯品牌 logo 或可辨識人臉的照片，以免肖像權／商標爭議。
- 建議尺寸：封面圖 1200×630（適合社群分享），內文圖寬度 800 以上。
- 下載後改成文章需要的檔名（如 `cover.png`），放進 `blog/assets/<slug>/`。

### Step 4 — 產生

雙擊 `build-blog.bat`（或在此資料夾執行 `npm run blog`）。
它會產生／更新：各文章頁、列表頁 `blog/index.html`、`sitemap.xml`。
產生後可先用瀏覽器打開 `blog/<slug>.html` 預覽，確認沒問題再上傳。

### Step 5 — 發布（用 GitHub Desktop）

用 GitHub Desktop 把 `neowise-website` 的變更同步到 GitHub（BensonPai/neowise-website）：

1. 打開 GitHub Desktop，左側「Changes」會列出這次改動的檔案。
2. **確認變動的檔案都有勾選**，特別是 `blog/`、`style.css`、`sitemap.xml`
   （通常預設全選，照著就好；漏傳會造成線上排版或內容不一致）。
3. 下方填一句 commit 說明（例如「新增文章：AI 與機器視覺的未來」），按 **Commit to master**。
4. 右上按 **Push origin** 推上 GitHub。

推上去後，GitHub Pages 會在幾分鐘內自動更新 https://neowise.com.tw 。

> 記憶口訣：**改 md → 按 build-blog.bat → GitHub Desktop 上傳**。少了中間那步，網頁就還是舊的。

---

## 四、文章檔格式（front-matter）

檔案最上方用 `---` 包起來的區塊，放 SEO 與圖片需求：

```markdown
---
title: "文章標題"
description: "SEO 摘要，60-155 字，含關鍵字"
keywords: ["關鍵字1", "關鍵字2", "關鍵字3"]
slug: "url-用英文與連字號"
date: "2026-09-11"
author: "炘智科技"
category: "技術文章"
status: "draft"
cover_alt: "封面圖替代文字"
images:
  - file: "cover.png"
    alt: "封面圖說明"
    usage: "文章頂部主視覺"
    prompt: "生圖提示或圖庫建議"
---

## 小標題

正文，支援 **粗體**、*斜體*、清單、> 引用、`程式碼`、[連結](網址)、圖片。

![圖片說明](assets/你的slug/cover.png)
```

支援的 Markdown 語法：
- 標題 `##`（→ h2）、`###`（→ h3）
- 清單（`-` 或 `1.`）
- 粗體 `**字**`、斜體 `*字*`、行內程式碼 `` `字` ``
- 引用 `> 內容`
- 分隔線 `---`
- 連結 `[文字](網址)`、圖片 `![說明](路徑)`

---

## 五、常見問題

**Q：文章沒出現在列表頁？**
A：檢查 `status` 是不是 `approved` 或 `published`。`draft` 不會發布。

**Q：改了 .md 但網站沒變？**
A：要重跑產生器（雙擊 `build-blog.bat`），再用 GitHub Desktop 同步到 GitHub。

**Q：圖片沒顯示？**
A：確認圖片放在 `blog/assets/<slug>/`，且檔名跟 `.md` 裡寫的一致。

**Q：可以刪文章嗎？**
A：刪掉 `blog-content/` 裡的 `.md`，並手動刪 `blog/<slug>.html`，再重跑產生器更新列表與 sitemap。

**Q：slug 是什麼？**
A：文章網址代稱，決定 `blog/<slug>.html` 與圖片資料夾。用英文小寫加連字號，例如 `smt-ai-defect-detection`。

---

## 六、之後可擴充（選配）

- **全自動產文**：接 OpenAI / Claude API，做一個題目輸入表單，自動生成草稿（需 API Key、費用）。
- **自動配圖**：串接生圖 API 自動產生圖片。
- **自動發布**：改用 GitHub Actions，push 後自動跑產生器並部署。

需要時再跟我說，我可以幫你加上去。
